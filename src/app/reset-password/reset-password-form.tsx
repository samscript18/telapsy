"use client";
import Link from "next/link";
import { useSearchParams } from "next/navigation";
import { useState } from "react";
export function ResetPasswordForm(){const token=useSearchParams().get("token")??"";const [message,setMessage]=useState("");const [error,setError]=useState("");const [done,setDone]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setError("");const form=new FormData(event.currentTarget);if(form.get("password")!==form.get("confirm")){setError("Passwords do not match.");return;}const response=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:form.get("password")})});const data=await response.json();if(!response.ok){setError(data.error);return;}setMessage(data.message);setDone(true);}return <div className="shell grid min-h-[620px] place-items-center py-16"><section className="card w-full max-w-lg p-8 sm:p-10"><p className="eyebrow">Choose a new password</p><h1 className="display mt-3 text-5xl">Fresh start.</h1>{!token?<p role="alert" className="mt-6 text-red-700">This reset link is missing its token.</p>:done?<div className="mt-7"><p role="status" className="rounded-xl bg-[#edf3df] p-4">{message}</p><Link href="/signin" className="btn btn-primary mt-5 w-full">Sign in</Link></div>:<form onSubmit={submit} className="mt-8 grid gap-5"><label className="grid gap-2 text-sm font-bold">New password<input name="password" type="password" className="field" minLength={8} required autoComplete="new-password"/></label><label className="grid gap-2 text-sm font-bold">Confirm new password<input name="confirm" type="password" className="field" minLength={8} required autoComplete="new-password"/></label>{error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-red-700">{error}</p>}<button className="btn btn-primary">Update password</button></form>}</section></div>}
